# Kagent-native MCP server dependencies.
# These are in addition to the shared/ module's transitive requirements
# (pymilvus, sentence-transformers, torch, numpy).

fastmcp>=2.0.0
pymilvus>=2.4.0
sentence-transformers>=2.2.0
