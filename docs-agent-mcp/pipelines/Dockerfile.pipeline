FROM python:3.11-slim

WORKDIR /app

COPY hugo_ingest.py canonical_rag_ingest.py utils.py milvus_store.py /app/

RUN pip install --no-cache-dir \
    requests \
    beautifulsoup4 \
    toml \
    pyyaml \
    pymilvus==2.6.14

ENV PYTHONPATH=/app
