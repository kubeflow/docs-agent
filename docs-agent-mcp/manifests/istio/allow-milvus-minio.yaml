apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: allow-milvus-minio
  namespace: ml-infra
spec:
  selector:
    matchLabels:
      app: minio
  action: ALLOW
  rules:
  - to:
    - operation:
        ports:
        - "9000"
        - "9001"
